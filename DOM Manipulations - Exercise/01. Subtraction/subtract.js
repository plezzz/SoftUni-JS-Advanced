function subtract() {
    let one = document.getElementById('firstNumber').value;
    let two = document.getElementById('secondNumber').value;
    document.getElementById('result').textContent =Number(one) - Number(two);
}
